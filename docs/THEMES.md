# Theme tokens

A theme is a JSON object. Every field is optional — anything you leave out
falls back to the default below, so the smallest useful theme is a couple of
colours. There is no per-theme CSS anywhere in the project: these tokens are
the entire vocabulary, and the built-in themes use nothing you cannot.

You do not have to write these by hand. Open `theme-builder.html` in a normal
browser, design what you want, and copy the code it gives you.

**This file is generated by `node tools/make-docs.js`. Edit `src/theme.js` instead.**

## Reference

### Paper

| Token | Default | Accepts | In Basics |
|---|---|---|---|
| `paper` | `"#feff9c"` | hex colour | yes |
| `paper2` | _derived_ | '' → derived as a slightly darker paper |  |
| `paperAngle` | `160` | 0 to 360 |  |
| `paperAlpha` | `1` | 0 to 1 |  |
| `texture` | `"none"` | none \| ruled \| grid \| dots \| scanlines \| grain | yes |
| `textureColor` | `"#000000"` | hex colour |  |
| `textureColor2` | `"#ffffff"` | hex colour |  |
| `textureAlpha` | `0.09` | 0 to 1 |  |
| `textureGap` | `32` | 2 to 80 |  |

### Ink

| Token | Default | Accepts | In Basics |
|---|---|---|---|
| `ink` | `"#3a3833"` | hex colour | yes |
| `inkSoftAlpha` | `0.5` | 0 to 1 |  |
| `accent` | `"#d64545"` | hex colour | yes |
| `font` | `"hand"` | hand \| sans \| condensed \| system \| serif \| mono \| marker | yes |
| `textShadow` | `0` | 0..1 dark drop shadow, for legibility over video |  |
| `glow` | `0` | 0..1 emissive halo, tinted per element |  |

### Shape

| Token | Default | Accepts | In Basics |
|---|---|---|---|
| `width` | `380` | 180 to 900 | yes |
| `radius` | `2` | 0 to 40 |  |
| `tilt` | `-2` | -15 to 15 | yes |
| `shadow` | `"soft"` | none \| soft \| hard \| lift | yes |
| `padX` | `26` | 0 to 80 |  |
| `padY` | `24` | 0 to 80 |  |
| `padBottom` | `16` | 0 to 80 |  |
| `padLeft` | `0` | 0 → same as padX |  |
| `borderWidth` | `0` | 0 to 8 |  |
| `borderColor` | `"#000000"` | hex colour |  |
| `borderAlpha` | `0.18` | 0 to 1 |  |
| `frame` | `"none"` | none \| brackets \| rule-left \| inset |  |
| `frameColor` | `"#DC3E30"` | hex colour |  |
| `frameAlpha` | `1` | 0 to 1 |  |

### Corner

| Token | Default | Accepts | In Basics |
|---|---|---|---|
| `corner` | `"fold"` | none \| fold \| dogear \| curl \| torn | yes |
| `cornerAt` | `"br"` | tl \| tr \| bl \| br | yes |
| `cornerSize` | `32` | 0 to 90 |  |
| `curl` | _derived_ | '' → derived from paper |  |
| `pin` | `"none"` | none \| tape \| pushpin | yes |
| `pinColor` | `"#f4f1e4"` | hex colour |  |
| `pinAlpha` | `0.75` | 0 to 1 |  |

### Title

| Token | Default | Accepts | In Basics |
|---|---|---|---|
| `titleFont` | `"inherit"` | inherit \| hand \| sans \| condensed \| system \| serif \| mono \| marker |  |
| `titleSize` | `34` | 8 to 90 | yes |
| `titleWeight` | `700` | 100 to 900 |  |
| `titleCase` | `"none"` | none \| upper |  |
| `titleTrack` | `0` | -2 to 10 |  |
| `titleColor` | _derived_ | '' → ink |  |
| `titleRule` | `false` | true \| false |  |

### Tasks

| Token | Default | Accepts | In Basics |
|---|---|---|---|
| `taskSize` | `26` | 8 to 70 | yes |
| `taskLine` | `32` | 8 to 90 |  |
| `check` | `"square"` | square \| sharp \| circle \| none | yes |
| `checkSize` | `22` | 8 to 44 |  |
| `checkStroke` | `2` | 0 to 6 |  |
| `checkColor` | _derived_ | '' → ink |  |
| `checkFillDone` | _derived_ | '' → stays transparent when checked |  |
| `checkTick` | _derived_ | '' → checkColor; set it when the box fills in |  |
| `checkTickWidth` | `3.2` | 0.5 to 8 |  |
| `checkTickScale` | `1.18` | tick size relative to the box; >1 overhangs |  |
| `checkJitter` | `true` | true \| false |  |
| `doneStrike` | `true` | true \| false | yes |
| `doneFade` | `"dim"` | none \| dim \| mute | yes |
| `doneDim` | `0.55` | 0.1 to 1 |  |
| `strikeWidth` | `2.5` | 0.5 to 8 |  |

### Progress & stamp

| Token | Default | Accepts | In Basics |
|---|---|---|---|
| `progress` | `true` | true \| false |  |
| `progressSize` | `20` | 8 to 48 |  |
| `progressColor` | _derived_ | '' -> soft ink |  |
| `progressWeight` | `400` | 100 to 900 |  |
| `stampText` | `"All done!"` | text |  |
| `stampSize` | `32` | 8 to 80 |  |
| `stampTrack` | `1` | -2 to 12 |  |
| `stampRadius` | `8` | 0 to 40 |  |
| `celebrate` | `true` | true \| false |  |

## Derived colours

Tokens marked _derived_ work themselves out from another colour when left
empty — `paper2` and `curl` from `paper`, `titleColor` and `checkColor` from
`ink`. Set one explicitly and it stops deriving. In the builder these are the
fields with an **Auto** tick next to them.

Deriving happens in JavaScript rather than in CSS (`color-mix()` and relative
colour syntax need a newer Chromium than some shipping OBS builds carry), so a
theme renders the same on old and new OBS alike.

## Sharing a theme

The builder produces a code starting `sn1:` — base64 of the JSON, carrying
only the fields that differ from the defaults. Paste it into the note's
**Theme** panel in the OBS dock. Raw JSON works there too, if you would rather
hand-edit.

Codes are self-contained, so a theme is one paste to share and does not depend
on the version of the note that made it: unknown fields are ignored and missing
ones take their defaults.

## Built-in themes

| Theme | `id` | Tokens it sets |
|---|---|---|
| Classic Yellow | `yellow` | 5 |
| Pink | `pink` | 5 |
| Blue | `blue` | 5 |
| Green | `green` | 5 |
| Lined Notepad | `lined` | 12 |
| Kraft | `kraft` | 9 |
| Midnight | `midnight` | 5 |
| Asylum Life | `asylum` | 54 |

**Asylum Life** is the one that shaped this list. It is not a repaint: no tilt,
no folded corner, a semi-transparent panel with corner brackets, a condensed
uppercase title and a different font. It is also plain data, like all the
others — if it had needed one line of its own CSS, the token set would have
been wrong.
